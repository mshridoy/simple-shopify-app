import React from "react";

export default function UnAuthUser() {
  return (
    <div
      style={{
        display: "flex",
        height: "100vh",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      You are unAuth
    </div>
  );
}
